#include <stdio.h>
#include <time.h>
#include "mymalloc.h"
/*
 * Runs workloads, calculates and output mean time for each workload
*/
int main(int argc, char** argv) {
    char *ptr = malloc(64);
    return 0;
}